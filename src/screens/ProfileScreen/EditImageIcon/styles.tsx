import { StyleSheet } from "react-native";

export const editImageStyles = StyleSheet.create({
  container: {
    position: "relative",
    alignItems: "center",
    justifyContent: "center",
  },
  backgroundIcon: {
    position: "absolute",
  },
});
